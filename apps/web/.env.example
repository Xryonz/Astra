# API endpoint (sem barra final)
VITE_API_URL="http://localhost:3001"

# ── Sentry (opcional) ─────────────────────────────────────────
# Pega DSN em sentry.io → projeto React/Browser → Settings → Client Keys
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=
VITE_SENTRY_TRACES_SAMPLE=0.1
VITE_RELEASE=
