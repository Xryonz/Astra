# Astra — Roadmap (compacto)

Foco: desempenho maximo · engenharia de ponta · custo zero.
Produto que SHIPA agora = hibrido React/Capacitor (grupo de testers).
Native Kotlin = track paralelo de longo prazo (nao bloqueia o ship).
Regra: nunca pular etapas; perguntar a cada passo como progredir.

## T1 — Hibrido React/Capacitor  [SHIPA AGORA]
- [ ] LiveKit 60fps screenshare: maxFramerate 60, maxBitrate↑, simulcast OFF na track de tela, codec H264/VP8 c/ HW accel (ScreenShareCaptureOptions + PublishOptions)
- [ ] Native: camera (@capacitor/camera) — avatar/anexo
- [ ] Native: receber compartilhamento (Android intent filter → manda em DM)
- [ ] Native: localizacao na mensagem (@capacitor/geolocation)
- [ ] Anim #2: reduce-motion (2o toggle da aba Acessibilidade)
- [ ] Anim #5: LazyMotion (corta KB do bundle motion)
- [ ] Anim #6: virtualizar MessageList (@tanstack/react-virtual)
- [ ] Perf refactor: memo + selectors finos + code-split (NAO "so dividir componente")
- [ ] Build: 1 APK dev (debug) + 1 APK prod (release ASSINADO p/ distribuir)
- [ ] BUG (adiado): durante screenshare, os cards de participantes na call bugam (aparece "1 risco/linha" por usuario). So na web. Achado testando o M6c nativo (que recebe ok). Revisitar depois do nativo.

## T2 — Native Kotlin  /mobile-native  [EM ANDAMENTO — decisao: encerrar Capacitor, migrar p/ Kotlin]
- Clean Arch MVVM: data / domain / presentation (Composables + ViewModels)
- Rede: Retrofit + kotlinx.serialization + WebSocket · DI: Hilt · async: Coroutines · img: Coil
- Consome o backend existente. Capacitor fica CONGELADO como shippable ate paridade.
- Desktop futuro: Compose Multiplatform
- Toolchain casado c/ Capacitor: Gradle 8.11.1 · AGP 8.7.2 · compileSdk 35 · JDK 17 · Kotlin 2.0.21
- [x] M1 fundacao (Gradle+catalog, Compose, Hilt, tema Astra dark/amber, app roda vazio)
- [x] M2 rede (Retrofit+OkHttp+AuthInterceptor+TokenStore DataStore, modulos Hilt). BASE_URL = umbra-api-production.up.railway.app (mesma do Vite)
- [x] M3 slice Auth: login (POST api/auth/login) → Clean Arch (dto/api → repo → usecase → VM → Compose), TokenStore persiste sessao, NavHost gate login/home, TokenAuthenticator faz refresh transparente no 401 (RefreshApi em client pelado p/ evitar loop). Compila+KSP/Hilt OK. Login real VALIDADO no emulador (sessao ativa).
- [x] M4 WebSocket+DMs VALIDADO (realtime bidirecional no device): M4a SocketManager [chip Online] · M4b lista de DMs · M4c chat DM (historico + join_dm/new_dm + enviar + dedupe por id + abrir por @username + bolhas mine/other + userId no login)
  - Socket robusto: (1) auto-recupera token expirado no connect_error de auth (refresca via RefreshApi, cooldown 10s, deixa a auto-reconexao da lib usar o token novo); (2) re-join das conversas abertas no EVENT_CONNECT (rooms sao server-side, somem na queda — era o motivo de nao receber new_dm apos cair). activeRooms rastreado em join/leave.
- [x] M5 servers/canais VALIDADO: M5a lista de servidores + canais (GET /api/servers ja traz channels aninhados + _count; criar servidor por nome cria #geral junto; voz desabilitado=M6) · M5b chat de canal (GET/POST /api/channels/:id/messages + join_channel/new_message/message_deleted, dedupe por id, reusa o motor do DM). Enter envia / Shift+Enter quebra linha nos dois chats.
  - Web: liberado "Adicionar membro" por @username pra servidor (nao so grupo) — invite/:username nao exige amizade; opt-out de estranhos = config futura.
- [x] M6 LiveKit Android — CORE NATIVO COMPLETO (voz, video de camera, screenshare). So a fluidez 60fps do screenshare falta confirmar em device fisico (emulador limita). Fatiado, cada slice validado com 2 contas:
  - [x] M6a so audio VALIDADO no device (falar/ouvir + sobrevive background): tocar canal voz -> POST /api/voice/token -> LiveKit.create + room.connect -> setMicrophoneEnabled. Audio remoto toca sozinho (SDK). CallService foreground type=microphone mantem a call em background. Permissao RECORD_AUDIO pedida antes de entrar (FGS exige). io.livekit:livekit-android 2.26.0 + desugaring (java.time em minSdk 24) + jitpack repo. Rota call/{channelId}. events via io.livekit.android.events.collect. Confirma: LIVEKIT_* setado na Railway (token conectou).
  - [x] M6b painel de participantes VALIDADO no device: lista quem esta na sala com avatar+nome real (resolvido via GET /api/servers/:id/members; identity do LiveKit = userId), anel ambar em quem fala (isSpeaking via eventos), icone mic mudo por pessoa, botoes Mutar/Silenciar(deafen)/Sair. Deafen = setVolume 0.0 nas RemoteAudioTrack + corta o proprio mic (estilo Discord). VoiceManager passou a expor participants[] e refaz snapshot em qualquer RoomEvent. serverId passa na rota call/ pra resolver nomes.
  - [x] M6c video de camera VALIDADO no device (publish + receive entre nativo e web): permissao CAMERA (runtime, opcional ao entrar), toggleCamera -> setCameraEnabled. Painel virou grid 2 col de tiles (aspect 1:1): mostra a camera (VideoTrackView) se ligada, senao cai pro avatar; anel ambar em quem fala, nome+mic mudo no rodape. CallParticipant ganhou cameraEnabled+videoTrack (getTrackPublication(CAMERA)). Render via io.livekit:livekit-android-compose-components:2.4.0 (VideoTrackView overload de VideoTrack cru + passedRoom, sem RoomScope). Camera so com a tela aberta (FGS camera fica pro futuro).
  - [~] M6d screenshare (fatiado):
    - [x] M6d.1 RECEBER tela VALIDADO no device (web compartilha -> nativo ve): snapshot pega tambem source SCREEN_SHARE; quem compartilha ganha um tile extra no grid (span 2 col, aspect 16:9, ScaleType.FitInside, label "(tela)").
    - [x] M6d.2a TRANSMITIR tela VALIDADO no device (nativo compartilha -> web ve): botao Tela -> dialogo MediaProjection (createScreenCaptureIntent, StartActivityForResult) -> setScreenShareEnabled(true, ScreenCaptureParams(resultData, notificationId, notification)). O LiveKit roda o PROPRIO foreground service (ScreenCaptureService type=mediaProjection, ja no manifesto da lib — confirmado no merge final). ControlBar virou 2 linhas (Mutar/Surdo + Camera/Tela) + Sair. Encoding ainda no default.
    - [x] M6d.2b tuning 60fps "gamer" — config aplicada + transmite OK (nativo->web). RoomOptions.screenShareTrackPublishDefaults = VideoTrackPublishDefaults(VideoEncoding(8Mbps, 60fps), simulcast=false) no LiveKit.create. VideoEncoding e (maxBitrate, maxFps). FLUIDEZ pendente de DEVICE FISICO: testado no emulador engasga (captura+encode na CPU virtual nao sustenta fps alto — limitacao do emulador, nao do codigo).
      - Levantado p/ ajustar SE engasgar no device real: (1) so setei maxFps de PUBLISH, nao o de CAPTURA -> screenShareTrackCaptureDefaults com maxFps alto; (2) trocar codec do screenshare pra H264 (encoder HW do celular sustenta 60fps melhor que VP8 software). Nao aplicado agora pra nao tunar as cegas no emulador.
- [~] M7 Design editorial-cosmico (paridade visual com o web; user pediu "copiar tudo do Capacitor, otimizar com Compose, nao se conter nas animacoes"). Accent = prata-estelar #d4d8e0 (NAO mais ambar; web migrou Umbra->Astra cosmic). Fatiado:
  - [~] F1 Fundacao ESCRITO+APK OK (falta validar): tokens cosmicos (6 camadas bg void->active, accent prata, 3 niveis texto, semanticos) em Color.kt + AstraColors holder via LocalAstraColors. 4 fontes .ttf em res/font (DM Serif Display, DM Sans VARIABLE c/ FontVariation, DM Mono, Great Vibes) + tipografia editorial no Material3 (serif titulos, sans corpo) -> app inteiro ja herda. Theme mapeia tudo no darkColorScheme.
  - [x] F2 Atmosfera + primitivas ESCRITO+APK OK: StarField.kt (1 Canvas: 70 stars drift global 90s + 14 twinkle seamless via sin(angulo) + 3 meteoros raros 24s) + CosmicBackground wrapper. Editorial.kt (Reveal stagger fade+lift, MarginaliaLabel mono, RomanNumeral serif italic, HairlineRule). Motion.kt (easings EaseSpring/OutSoft/Snappy do web). Splash ja usa CosmicBackground + wordmark Great Vibes 76sp c/ glow.
  - [x] Telas TODAS redesenhadas (1a passada; falta validar no device + ajustes): Splash, Login (hero), Home (hub: cards editoriais I/II + socket chip), DM list / Server list / Channel list (EditorialTopBar compartilhado + AstraAvatar/EmptyState/CosmicSpinner em CommonUi), chat DM+canal (motor Chat.kt compartilhado: MessageBubble com slide-in direcional + overshoot + starlight sweep gated, ChatInputBar pill + botao circular, ChatMessageList; matou a duplicacao DM/Channel), Call (tiles cosmicos, anel prata em quem fala, CallToggle pill com estado por preenchimento). Componentes em ui/components: StarField, CosmicBackground, Editorial (Reveal/Marginalia/Roman/Hairline), EditorialTopBar, CommonUi, Chat. Logica das telas intacta.
- [~] M8 Chat rico (paridade de interacoes; user pediu "replique tudo do Capacitor, acabe tudo primeiro"). Backend de DM e mais magro que o de canal -> DM so recebe o que o server suporta (paridade com o web). Fatiado:
  - [x] M8a editar/apagar: canal edita (PATCH, banner editando, label "editado", socket message_edited) + apaga (DELETE); DM so apaga (sem PATCH no backend). Menu via combinedClickable+DropdownMenu; helpers EditingBanner/DeleteMessageDialog no Chat.kt; canEdit gateia.
  - [x] M8b reacoes (SO canal): long-press -> 6 emojis rapidos; chips FlowRow sob a bolha (borda prata = eu reagi); POST .../react toggle + socket reaction_update; mine via uid em users[]. DM nao tem reacao no backend.
  - [x] M8c responder (canal+DM): menu Responder + ReplyBanner + quote do pai no topo da bolha; replyToId no POST; replyTo ja vinha do GET/socket.
  - [x] M8d digitando (canal+DM): emite typing_start na 1a tecla / stop apos 3s ou no envio; escuta user_typing/dm_user_typing, agrega por userId (stop nao traz username) c/ expiry 6s; TypingIndicator acima do composer.
  - [x] M8f fixar (SO canal): menu Fixar/Desafixar + indicador 📌 + acao 📌 abre dialog de fixadas (GET /pinned); POST/DELETE .../pin + socket message_pinned.
  - [x] M8e nao-lidas (canal+DM): o GET /api/servers JA devolvia lastMessageAt por canal (nativo so jogava fora no ignoreUnknownKeys) e as rotas /reads ja existiam -> ZERO mudanca no backend. Canal: junta lastMessageAt + /reads/channels = dot; markRead ao abrir; channel_activity ao vivo; markSeen otimista no clique. DM: lastMessage.createdAt + /reads/dm (mine) + lastFromMe (nao marca msg propria); markRead ao abrir; new_dm de outro ao vivo. Dot prata nas 2 listas.
  - M8 COMPLETO (6/6): todos compilam + APK OK; falta validar no device.
- [~] M9 Contas+social (paridade com o web; register/amigos/perfil + Home virou hub). Compila + assembleDebug OK; falta validar no device. Fatiado:
  - [x] M9a Registro: RegisterScreen/VM/UiState + RegisterUseCase; POST api/auth/register (201, mesmo envelope user+tokens do login; 409 email/username em uso, 400 validacao -> erro amigavel do corpo). LoginScreen enxugado p/ usar AuthFields compartilhado + link "criar conta" (onGoToRegister). Rota register no AstraApp.
  - [x] M9b Amigos: FriendsApi/Module/Dtos + Friend/FriendRequest + FriendsRepository(+Impl) + FriendsScreen/VM/UiState. GET api/friends · /requests (incoming) · /outgoing; POST /request (404 nao achou, 400 voce mesmo), /{id}/accept; DELETE /{id}. Abre perfil do amigo (-> user/{id}).
  - [x] M9c Perfil/Conta: UserApi/UserModule/UserDtos + Profile/ProfileView + UserRepository(+Impl, me() cacheia em Singleton, forceRefresh). GET api/auth/me (email+hasPassword) · GET api/profile/{id} (publico + servers em comum + status) · PATCH api/profile (campos null=nao mexe, ""=limpa; 409 username em uso) · POST api/auth/password. Telas: Settings, Account, ProfileEdit, UserProfile (cada uma VM+UiState).
  - [x] M9d Home hub: Row[ ServerRail | MainPanel ]. Carrega servers+conversations+dmReads+me em paralelo. Faixa "na voz" (voicePresence -> ActiveVoiceCard, tap entra na call via onJoinVoice) · DMs com dot prata de nao-lido + markSeen no tap · FAB ✎ nova mensagem (open por @username -> opened -> navega). Skeleton.kt (ListSkeleton) p/ loading.
  - [x] FIX: KDoc `[userId...]` em ServerRepository.kt quebrava kspKotlin ("Closing bracket expected"; Kotlin parseia `[ref]` como link de doc). Colchetes removidos.
  - [x] M9e Home paridade Discord (user pediu igual a referencia): header serif "Mensagens" + botao busca (filtro local inline) + pill "Adicionar amigos"; faixa "na voz" virou LazyRow de cards; DM rows c/ timestamp relativo (relativeShort via java.time, desugaring) + prefixo "Você:" qd lastFromMe + dot prata; BottomUserBar (avatar->settings, status do socket, sino); FAB compose. Glyphs monocromaticos em Canvas (Search/PersonAdd/Bell/Compose) no lugar de Material Icons (sem dep nova). Transicoes de rota do AstraApp realinhadas ao PageTransition do web: fade + slide-x curto (EaseSpring 0.16,1,0.3,1), exit rapido, SEM scale.
- [~] M9f Convites (entrar em servidor + compartilhar). assembleDebug OK; falta validar no device. inviteCode passou a ser mapeado no ServerDto/Server (GET /api/servers ja devolvia). InvitesApi (GET /api/invites/:code preview, POST /:code/join) + InvitesRepository(+Impl, parse de erro 403/409/404) + InvitesModule. JoinServerScreen (cola link OU codigo cru -> extractCode tira /invite/ ou /i/ -> preview com card do servidor -> Entrar -> navega pros canais). Entrada em ServerListScreen ("Entrar com um convite"). Compartilhar: acao "Convidar" na ChannelListScreen -> share sheet do Android com link ${BASE_URL}/i/:code (mesma pagina OG que o web compartilha). FALTA (proximo): deep link (intent-filter astra:// + https) pra abrir convite tocando no link; regenerar codigo (owner).
- [ ] M10 — AUDITORIA WEB -> PARIDADE TOTAL (pedido do user): varrer apps/web INTEIRO (rotas, componentes, stores, libs, features) e portar pro nativo TODA feature que ainda falta. Entregar 1o um INVENTARIO (tabela: feature web | existe no nativo? | esforco) -> validar com o user -> fechar gaps em fatias priorizadas. Gaps ja conhecidos: anexos/imagens em msg, config de servidor (roles/permissoes/membros), discover, threads/polls/emojis custom, onboarding cosmico, notificacoes push, OAuth Google, deep link de convite. Regra: nada de port as cegas — ler o codigo web antes, fatiar, perguntar a cada passo.
- M10 — DECISAO 2026-07-04 (user): CORTADOS do plano nativo = mensagens salvas (bookmarks), lembretes e threads (rotas backend ficam, o web usa). Ordem final da paridade: (1) config de servidor completa (cargos/permissoes/bans/membros + emojis custom), (2) busca global, (3) miudezas (Google no login nativo, regenerar convite, recado na lista de membros, pull-to-refresh se couber). Depois: retocar design (pedido do user: "tem coisas muito simples") -> blocos estruturais (Room etc).
- AUTH/Google (nativo, futuro): Google so LOGA conta existente, NUNCA cria conta — ja e politica de seguranca do backend (passport.ts: email nao-registrado e bloqueado com code email_not_registered). No nativo, ao adicionar "Entrar com Google", tratar o deep link astra://login?error=google_email_unregistered -> mandar pro registro. Nao ha nada a mudar no backend.
- DECISAO: Capacitor CONGELADO ja; camera/share/localizacao/animacoes serao nativos no Kotlin (nao no Capacitor)

## T3 — Dados/Infra  [QUANDO O VOLUME PEDIR]
- Postgres (transacional) · Redis (presenca/sessao) · TimescaleDB (historico msgs, particao temporal por channel_id, created_at)
- Free tier: Render/Zeabur (API) · Supabase (PG/Timescale) · Upstash (Redis)
- Agora: tabela messages "partition-ready"; migrar p/ Timescale so sob volume

## Decisoes de planejamento — Rodada 1 (2026-06-27)
- **RikkaUI:** adotar via copy-paste/CLI, MIGRACAO GRADUAL (componente novo nasce RikkaUI; Material3 sai aos poucos). Nao refatorar tudo de uma vez.
- **Criptografia:** E2EE so nas DMs e DEPOIS (canais ficam legiveis pro servidor -> preserva IA-resumo + busca). Agora: endurecer o basico.
- **Proximo bloco:** Room + offline-first (UI le do banco local por Flow; rede so escreve). Base pro resto.
- **Features T5 mantidas (todas):** IA resumir canal, aba de plugins, modo LAN, eco-mode. Swipe entra como win barato no fluxo normal de UI.

### Rodada 2 (detalhe do bloco + sequencia)
- **Banco local:** Room com KMP (2.7+ suporta Android + JVM desktop -> nao bloqueia o T4). Integra com Coroutines/Flow ja em uso.
- **Offline-first MVP:** read-cache do chat (DMs+canais leem do Room instantaneo; socket/REST so ESCREVEM no Room; UI nunca espera rede). Envio offline (outbox) fica pra fase 2.
- **Desktop/KMP:** so DEPOIS da paridade mobile (extrair shared, Retrofit->Ktor, Hilt->Koin).
- **Ordem T5:** IA resumir canal (1o, reusa Claude) -> eco-mode -> modo LAN -> aba de plugins (ultimo).

## Ordem de execucao (plano sequenciado — pos device-test)
0. **Validar no device** o que ja shipou (auth, Home polish, perfil, 4c) + **redeploy da API** (fix do banner).
1. **Room + offline-first (read-cache do chat):** Room KMP como fonte da verdade; UI observa `Flow`; socket/REST escrevem no Room. Sem envio offline ainda.
2. **IA resumir canal:** `POST /api/channels/:id/summarize?since=<lastRead>` (reusa Claude do bot.ts) + botao no header do canal -> sheet com resumo.
3. **Eco-mode:** toggle pausa anim / baixa polling / congela GIF; auto em bateria baixa.
4. **Modo LAN:** descoberta mDNS/NSD + chat texto P2P (voz depois).
5. **Aba de plugins:** API estavel + sandbox + escopo de permissao (read-only primeiro).
- Transversal: RikkaUI copy-paste em componente NOVO (Material3 sai aos poucos); swipe como win barato; E2EE so DMs quando o produto pedir.
- So apos paridade mobile: desktop/KMP (shared, Ktor, Koin, Compose MP, Conveyor, toggle HW-accel; LiveKit desktop = problema em aberto).

### Room read-cache — fatiamento (quando comecar o bloco 1)
- R1: dep Room KMP + entidades (MessageEntity/ConversationEntity) + DAO + DB.
- R2: chat DM le do Room (Flow) -> abre instantaneo; REST/socket escrevem no Room (nao mais direto na UI).
- R3: chat de canal igual (reusa o motor).
- R4: cache de imagens/anexos (Coil disk cache) + retencao (limite por conversa).

## T4 — Desktop nativo + KMP  [FUTURO — depois da paridade mobile]
Meta: reaproveitar o Kotlin do /mobile-native no desktop sem reescrever.
- **KMP (Kotlin Multiplatform)** — mover `data/` + `domain/` (repos, use cases, models, DTOs) pra um modulo `shared` que compila pra Android + JVM Desktop. Comecar extraindo 1 feature pequena (auth) pro shared, validar nos 2 targets, migrar o resto. Tensao: Hilt e Retrofit sao Android-only (ver T6) — trocar antes.
- **Compose Multiplatform** — as telas Compose viram janela desktop quase sem mudar. Usar `org.jetbrains.compose`; APIs Android (Toast, Context, MediaProjection) viram `expect/actual`. StarField/CosmicBackground/Editorial migram direto (Canvas e multiplataforma).
- **Ktor client** — substitui o Retrofit (Android-only) na rede do shared. `HttpClient` + `ContentNegotiation(kotlinx.serialization)` + engine OkHttp (Android) / CIO (desktop). Pre-requisito real do T4; mantem os mesmos DTOs.
- **LiveKit Kotlin SDK** — no Android e `io.livekit:livekit-android` (ja usado, M6). ATENCAO: esse SDK e Android-only (WebRTC empacotado pra Android). Voz/video no desktop NAO sai de graca do Compose MP — sem SDK JVM oficial; opcoes: WebView com o JS SDK, ou processo nativo. Decidir antes de prometer call no desktop.
- **Conveyor (Hydraulic)** — gera instaladores assinados (.exe/.msi, .dmg, .deb) + auto-update do Compose Desktop. `conveyor.conf` aponta pra distribuicao do Compose Desktop; CI cospe os arquivos de download. Custo zero pra open-source.
- **Config desktop: aceleracao de hardware** — toggle nas Configuracoes do desktop pra render via GPU. Compose Desktop ja usa Skia; expor o backend de render (ex: `skiko.renderApi`) numa setting, com fallback software se a GPU bugar. [anotado]

## T5 — Features de produto  [BACKLOG]
- **Aba de plugins** — extensibilidade tipo marketplace interno. Definir API estavel (eventos onMessage, slash-commands, paineis) e rodar plugin em sandbox (JS via QuickJS/J2V8, ou modulo Kotlin assinado). Comecar read-only (plugin so le eventos) antes de dar escrita. Risco: seguranca — plugin de terceiro precisa de escopo de permissao, nao le tudo.
- **IA "resumir canal"** — botao que le e resume o que rolou enquanto voce esteve offline. Ja existe IA no backend (`apps/api/src/lib/bot.ts` usa Claude). Endpoint novo `POST /api/channels/:id/summarize?since=<lastReadAt>`: pega as msgs nao-lidas, manda pro Claude com prompt de resumo, devolve texto; mobile poe um botao no header do canal -> resumo num sheet. TENSAO: exige o servidor LER as msgs — incompativel com E2EE total (ver criptografia).
- **Modo LAN** — conversar/voz na rede local sem internet. Descoberta via mDNS/NSD (Network Service Discovery), um peer vira host efemero, msgs P2P. Escopo grande — comecar por "achar peers na LAN + chat texto"; voz LAN depois. Bom pra LAN party.
- **Eco-mode** — toggle que poupa bateria/CPU: pausa animacoes (StarField/Reveal), baixa polling/heartbeat, congela GIFs no 1o frame, reduz qualidade de video na call. Irmao do reduce-motion mas pra energia; liga sozinho em bateria baixa (`BatteryManager` <20%).
- **Swipe (Jetpack Compose)** — gesto pra economizar clique. `SwipeToDismissBox` / `anchoredDraggable` nas linhas: swipe na DM/canal -> acao rapida (responder/marcar lido/arquivar); swipe na msg -> responder. Editorial: revelar a acao com accent atras. Cuidado: nao brigar com scroll horizontal vizinho.
- **Melhorar a criptografia** — hoje: TLS em transito + bcrypt na senha. Proximo nivel = E2EE (msgs cifradas ponta-a-ponta) via libsignal (protocolo Signal) ou NaCl/age; chave no dispositivo (Keystore Android / equivalente desktop), servidor so guarda blob cifrado. TENSAO PESADA: quebra busca no servidor, resumo por IA e preview de link (tudo que LE a msg). Decidir: E2EE so em DM (canais legiveis pra IA/busca) ou abrir mao do resumo server-side.

## T6 — Local-first / dados / DI  [BACKLOG — parte ja em uso]
- **Room database** — cache local de mensagens + imagens (abre instantaneo, funciona offline). Room como fonte unica da verdade: a UI observa `Flow<List<Message>>` do Room; socket/REST so ESCREVEM no Room; a tela nunca espera a rede. No KMP, Room ja tem suporte multiplataforma (ou SQLDelight como alternativa desktop).
- **Melhorar offline-first** — outbox pattern: msg enviada offline entra numa fila local (Room) com status "enviando", UI otimista; um worker (WorkManager no Android) drena a fila quando a conexao volta; reconcilia por id pra nao duplicar. Casa direto com o Room.
- **Coroutines & Flow** — JA em uso (Retrofit suspend, SocketManager como SharedFlow, StateFlow nos VMs). Proximo: ligar o Room por Flow pra UI atualizar sozinha quando o cache muda (sem refetch manual) — e a cola do offline-first.
- **Hilt** — JA em uso (DI do Android). Atencao pro KMP: Hilt e Android-only. Ao extrair o `shared`, a DI dele vira Koin ou manual. Padrao comum: Koin no shared, Hilt so na borda Android. Decidir no T4.

## T7 — Polimento + Otimizacao final  [AO FIM DO ROADMAP — app liso em TODO device]
Meta (pedido do user): a passada de otimizacao mais completa possivel pra rodar
liso em qualquer aparelho, low-end incluso. So depois das features fecharem.
- **Compose render:** estado que muda por frame so em modifier-lambda (graphicsLayer/offset/drawBehind), nunca na composicao; @Stable/@Immutable nos models de UI; remember nos calculos caros; cortar recomposicao em cascata (medir com Layout Inspector + recomposition counts).
- **Listas:** LazyColumn com key + contentType (texto/imagem/voz reciclam slots certos); derivedStateOf no scroll (botao "descer"/header); paginacao do historico (nao segurar tudo em memoria).
- **Imagens:** Coil com disk cache + downsample pro tamanho-alvo (nunca full-res); placeholder/blur leve.
- **Startup:** baseline profiles (ART) pra matar jank no 1o run; medir cold start; adiar init nao-critico; o splash ja cobre a carga.
- **Overdraw/GPU:** checar com debug de overdraw; blur/sweep so onde paga; StarField pausa fora da tela e em eco-mode.
- **Build release:** R8 full + shrinkResources + remover libs nao usadas; medir tamanho do APK.
- **Rede/IO:** dedupe de chamadas, read-cache no Room, backoff de socket; nada de bloqueio na main thread.
- **Medicao (regra):** perfilar em device low-end real com Macrobenchmark (jank %, cold start) ANTES e DEPOIS de cada mudanca — nunca otimizar as cegas.

## Stack de UI — RikkaUI  [user: copy-paste/CLI, migracao gradual]
- **O que e:** lib de componentes + design system pro Compose, estilo shadcn/ui (`compose.foundation` puro, ZERO Material3, Compose Multiplatform). v0.3.0 (pre-1.0). Repo rainxchzed/RikkaUi, docs rikkaui.dev. 2 modos: Gradle dep (`dev.rikkaui:foundation/components:0.3.0`) OU CLI copy-paste (voce dona o codigo-fonte).
- **Encaixe:** alinha com a filosofia do projeto (shadcn = primitivo + estetica editorial por cima) e com o desktop (Compose MP reusa). Substitui/encorpa a camada de primitivos do mobile.
- **Mentor — recomendacao:** adotar pelo modo COPY-PASTE/CLI (dona o snapshot) — insula da rotatividade de API pre-1.0 e casa com "editorial por cima do primitivo". NAO arrancar o Material3 de uma vez: usar RikkaUI em componente NOVO e migrar aos poucos (hoje o app usa M3 forte: ModalBottomSheet, AlertDialog, DropdownMenu, Button, MaterialTheme typography).
- **Tensao:** RikkaUI = foundation puro x app hoje = Material3. Migracao total e refactor grande -> gradual.

## Perf Compose — notas (do user, aplicar sempre)
- **Adiar leitura de estado:** estado que muda todo frame (posicao de menu deslizando, offset de swipe) vai em modifier-lambda (`Modifier.offset { IntOffset(x,0) }`, `graphicsLayer { }`) — le na fase de layout/draw, NAO na composicao -> nao recompoe a tela inteira por frame (mata o jank). Ja fazemos no `Reveal` (graphicsLayer).
- **LazyColumn:** sempre `key = { it.id }` (ja fazemos) + `contentType` por tipo de item (texto/imagem/audio) pro Compose reciclar os slots certos. Aplicar quando existir a MessageList nativa.
- **derivedStateOf:** estado derivado do scroll ("rolou pra cima?", "mostrar botao descer?") via `derivedStateOf { }` — so recompoe quando o booleano VIRA, nao a cada pixel. Usar no chat (botao "ir pro fim" / esconder header).

## Tensoes a decidir (mentor)
- **E2EE x IA/busca server-side:** cifrar ponta-a-ponta tira do servidor a leitura das msgs — mata "resumir canal" e busca. Escolher: E2EE so em DM (canais legiveis) OU abrir mao do resumo server-side.
- **Hilt/Retrofit x KMP:** ambos Android-only. O shared pede Koin + Ktor. Migrar a rede pro Ktor e o 1o passo concreto do desktop.
- **LiveKit desktop:** sem SDK JVM oficial — voz/video no desktop e problema em aberto; nao assumir "Compose MP = call no desktop de graca".
- **Plugins x seguranca:** sandbox + escopo de permissao antes de qualquer plugin de terceiro.

## Correcoes de mentor (voce pediu "me corrija")
- "Dividir componente" =/= mais rapido. Perf real = memoizacao + virtualizacao + code-split + menos re-render + bundle menor. Dividir so ajuda se cria fronteira de memo.
- Native Kotlin duplica o app inteiro: otimo p/ aprender, pesado como produto solo. Hibrido shipa; Kotlin e paralelo.
- TimescaleDB e prematuro com poucos usuarios; PG indexado aguenta milhoes de linhas. Projetar partition-ready agora, migrar depois.

## Skills sempre on
caveman (think) · primeval-zen (output) · karpathy (code) · vercel-react · shadcn

## Log de progresso
- 2026-07-05: LEVA 11 — CONFIG DE SERVIDOR fatia 2/6 CARGOS (06c3863). Secao "Cargos" no hub, gated MANAGE_ROLES (dono sempre). ServerRolesScreen: lista bolinha-da-cor + nome colorido + contagem de perms + apagar (confirm); "+ novo"/tocar cargo -> RoleEditorDialog (nome, cor via 6 swatches + hex livre com toggle "usar cor", hoist/separar-na-lista, e as 7 permissoes do backend como cards toggle com rotulo+descricao PT espelhando pt.json). Backend routes/roles.ts ja pronto -> ServerApi +5 (roles/create/update/delete + assign/unassign ja mapeados). DTOs RoleDto/RoleRequest + ServerMemberDto ganhou roles[]/topColor (members ja trazem cargos pra atribuicao). Otimista com revert. ATRIBUIR cargo a membro adiado pra fatia integrada com Membros (endpoints assign/unassign ja no ServerApi). FALTA device: criar/editar/apagar cargo, cor solida vs hex custom, toggles de permissao. AVISO DO USER: plano gratis da Railway acabando -> preciso passar recomendacao de backend (sem cartao: split Neon+Upstash+Render free tem cold-start ruim pra socket; VPS BR PIX ~R$27/mes e o caminho solido e ainda vira host do LiveKit). Proximas fatias: 3 banimentos, 4 visao geral completa, 5 emojis custom, 6 canais privados.
- 2026-07-04: LEVA 11 — CONFIG DE SERVIDOR fatia 1/6 (5cf83ee): hub + membros + sair/excluir (design validado via AskUserQuestion = hub com subtelas estilo Discord; user tambem CORTOU salvas/lembretes/threads do plano nativo e fixou ordem config-servidor -> busca global -> miudezas -> retoque de design). SERVER_EDIT virou ServerSettingsScreen (hub): header icone+nome, secoes gated por GET /servers/:id/me (Visao geral/Insignias so MANAGE_SERVER-admin-dono; Membros pra todos), rodape perigo (dono=Excluir, membro=Desorbitar, confirmacao, volta pra Home que ja recarrega no ON_RESUME). ServerEditScreen enxugou pra "Visao geral" (icone/nome/publico); insignias EXTRAIDAS pra ServerBadgesScreen/VM proprios (codigo movido 1:1). ServerMembersScreen nova: papel dono/admin visivel, menu por membro (Ver perfil / Tornar-Remover admin so dono / Expulsar confirm / Banir com motivo opcional), otimista com revert, regras do backend espelhadas (dono intocavel, admin so cai pela mao do dono, nunca a si mesmo). ServerApi +6 (me/setMemberRole/kick/ban/leave/delete); ServerMemberDto +id/role/nameColor; Server domain +isGroup (gotcha: DTO tinha, mapper jogava fora — Unresolved reference no 1o build). Engrenagem das orbitas agora pra TODO membro; isOwner orfao removido do ChannelListViewModel. PROXIMAS fatias: 2 cargos (CRUD cor/perms/hoist + atribuir), 3 banimentos (lista+revogar), 4 visao geral completa (banner/descricao/retencao/copiar-regenerar convite), 5 emojis custom (gestao + :nome: inline no chat + picker), 6 canais privados por cargo. FALTA device (leva 11): hub como membro comum vs dono, expulsar/banir/admin com 2 contas, desorbitar/excluir voltando pra Home.
- 2026-07-02: M10 — leva 6 (user escolheu 3 frentes via AskUserQuestion; device-tests adiados). (1) LIGACAO EM SUSSURRO (22088a5): fecha o ultimo Discord-core. Backend JA estava pronto (voice/token roomKind dm + socket dm_call_invite/accept/reject) -> fatia 100% cliente: SocketManager flows+emits (payload JSONObject identico ao web); rota CALL generica +kind (default channel, zero regressao) -> CallViewModel join(kind), remoto em DM usa nome da rota; caller = botao Phone no trailing da DmChat ("chamando..." 30s, aceito -> CallScreen, recusa/cancela para); callee = IncomingCallViewModel global no AstraApp (AstraDialog Atender/Recusar em qualquer tela, caller-cancel limpa, 30s auto-dismiss). (2) DEEP LINK CONVITE (fead995): intent-filter https host da API pathPrefix /i/ (sem autoVerify -> chooser "Abrir com Astra"); MainActivity -> DeepLinkBus (StateFlow, ponte Activity->composable) -> AstraApp consome QUANDO logado -> join?code=... ; JoinServerViewModel +SavedStateHandle -> preview automatico. (3) CODE BLOCKS (31db6e7): MessageContent divide o conteudo em segmentos — ```bloco``` vira caixa mono (fundo raised, borda, textAlign start, fonte 0.82x) e `inline` vira span mono, mencoes preservadas (InlineTokenRegex combina os dois). (4) ONBOARDING COSMICO (a7bad6b, divida do sweep de copy; user escolheu 5 slides + animacao completa): Constelacoes/Orbitas/Estrelas/Sussurros/Cometa (Lucide Sparkles/Orbit/Star/MessageCircle/Rocket); ConstellationGraphic re-desenha por slide (key(page) + rotationZ = ceu diferente) + icone scale/rotate + texto fade/sobe + AnimatedContent slide-x, gated reduceMotion; GATE COMPARTILHADO com o web: me() +onboardedAt (DTO/domain/mapper), HomeUiState.needsOnboarding one-shot (consumeOnboarding), concluir/pular -> POST api/auth/onboarded — quem viu no web nao ve de novo. TUDO assembleDebug verde, 4 commits. FALTA device: DM call 2 contas, tocar link de convite, msg com ```code```, onboarding (conta nova ou onboardedAt null).
- 2026-06-17: aba Acessibilidade+vibracao, DMs flat-rows, densidade canais, keystore p/ sessao, fundo void p/ gap teclado. Repo renomeado Umbra→Astra.
- 2026-06-17: mobile-native sync OK no Android Studio (tela Astra roda no emulador) = M1+M2 validados. Wrapper Gradle 8.11.1 copiado do projeto Capacitor. M3 (login nativo) escrito + compila (Navigation Compose escolhido p/ o gate de telas).
- 2026-06-18: M3 100% VALIDADO no emulador — login, credencial errada, persistencia (DataStore sobrevive fechar/reabrir), logout. Politica Google anotada.
- 2026-06-18: M4a SocketManager (socket.io-client 2.1.0, heartbeat 30s, re-auth no reconnect) VALIDADO runtime (chip Online no emulador).
- 2026-06-18: M4b lista de DMs VALIDADO (lista vazia OK pra conta nova). Bug socket offline (token expira no handshake) corrigido: refresh+reconnect automatico, chip volta Online sozinho.
- 2026-06-18: M4c chat DM escrito+compila — abrir conversa por @username (POST /api/dm/open), historico (GET messages), realtime via SocketManager (new_dm/dm_deleted como SharedFlow, join/leave), enviar (POST), dedupe por id, bolhas mine/other (userId persistido no DataStore no login).
- 2026-06-18: M4 100% VALIDADO no device — realtime bidirecional (mandar/receber ao vivo entre nativo e web). Fixados 2 buracos: socket caia por token expirado e nao recebia new_dm apos reconectar (faltava re-join das rooms). M4 inteiro fechado.
- 2026-06-18: M5a servidores + canais VALIDADO (criar servidor, ver #geral, navegar).
- 2026-06-18: M5b chat de canal escrito+compila — GET/POST /api/channels/:id/messages, realtime via SocketManager (new_message/message_deleted, join/leave_channel, re-join no connect), dedupe por id, bolhas mine/other. ChannelChatScreen duplica a UI do DM (extrair pra ui/chat compartilhado fica como cleanup futuro).
- 2026-06-18: M5 100% VALIDADO no device (chat de canal bidirecional com 2 contas). Enter envia / Shift+Enter quebra linha (fix nos 2 chats). Web: "Adicionar membro" por @username liberado pra servidor normal (skill kotlin-specialist instalada local, gitignored).
- 2026-06-18: M6a (voz so audio) ESCRITO + APK assembleDebug OK. LiveKit Android 2.26.0 (WebRTC nativo empacotado: liblkjingle_peerconnection_so.so), core library desugaring ligado, jitpack no settings. VoiceManager (Room unica, join/leave/toggleMic, state), CallService (foreground type=microphone), CallScreen pede RECORD_AUDIO antes de entrar. Canal de voz virou clicavel -> rota call/. Mesmo backend /api/voice/token do web.
- 2026-06-18: M6a 100% VALIDADO no device — voz nativa funcionando (falar/ouvir) e sobrevivendo ao background. LIVEKIT_* confirmado setado na Railway.
- 2026-06-18: M6b painel de participantes VALIDADO no device (2 contas): avatar+nome real (via /members), anel ambar em quem fala, icone mic mudo, deafen funcionando.
- 2026-06-18: M6c video de camera VALIDADO no device (publish+receive nativo<->web). Grid 2col de tiles (camera ou avatar), permissao CAMERA opcional. Achado bug SO-NA-WEB: cards bugam durante screenshare (adiado, ver T1). Foco segue 100% no nativo (depois do M6 -> app desktop nativo). Proximo: M6d (screenshare via MediaProjection).
- 2026-06-18: M6d.1 RECEBER tela VALIDADO no device (web compartilha -> nativo ve tile da tela).
- 2026-06-18: M6d.2a TRANSMITIR tela ESCRITO + APK OK. MediaProjection (createScreenCaptureIntent) -> setScreenShareEnabled(ScreenCaptureParams). LiveKit roda o proprio FGS (ScreenCaptureService type=mediaProjection da lib; confirmado MEDIA_PROJECTION+service no manifesto merged). VALIDADO no device (nativo compartilha -> web ve). Falta so M6d.2b (60fps) pra fechar o M6 -> app desktop nativo.
- 2026-06-20: Sidebar/Home "mais vida" (pedido do user, fatiado). Chunk 1: tela cheia (MainActivity sem Scaffold/padding -> CosmicBackground edge-to-edge; EditorialTopBar+CallScreen com statusBarsPadding), icone DM do rail -> Lucide.MessageCircle, tirado o FAB (compose foi pro header Conversas). Chunk 2: sistema de status com seletor (UserStatus enum ONLINE/IDLE/DND/INVISIBLE/OFFLINE; StatusDot.kt Canvas espelhando StatusDot.tsx; PATCH /api/profile/status no UserApi/Repository; HomeVM setStatus+logout) e card de perfil do rodape espelhando o UserFooter do web (avatar+dot, nome+status, DropdownMenu de status, botoes Pencil/Settings/LogOut). Tirado o texto online/offline. GAP v1: o proprio status nao e relido no cold start (auth/me nao devolve status; precisaria 1 linha no backend = deploy). Faltam Chunk 3 (abas Mensagens|Amigos) e Chunk 4 (icones de servidor). assembleDebug OK; falta device.
- 2026-06-20: Icones Lucide adotados (paridade com o web). Dep `com.composables:icons-lucide-android` PINADA em **1.1.0** — a 2.2.1 puxa kotlin-stdlib 2.2.21 e o KSP quebra ("incompatible version of Kotlin", projeto e Kotlin 2.0.21). Glyphs Canvas do Home trocados por Icon(Lucide.Search/UserPlus/Bell/SquarePen). assembleDebug OK. Telas novas daqui pra frente usam Lucide. NAO subir a versao da lib sem subir o Kotlin junto.
- 2026-06-20: M9f Convites + assembleDebug OK. Entrar em servidor por link/codigo (JoinServerScreen: cola -> preview -> Entrar; entrada na ServerListScreen) + compartilhar convite (acao Convidar na ChannelListScreen -> share sheet, link /i/:code). inviteCode mapeado no ServerDto/Server. InvitesApi/Repository/Module novos. Falta deep link e validar no device. Escolhido pelo user como 1a fatia do "migrar o resto"; faltam ainda anexos/imagens, config de servidor, discover, threads/polls/emojis/onboarding/notificacoes/OAuth.
- 2026-06-20: M9e Home paridade Discord + assembleDebug OK. Refeito HomeScreen pra bater com a referencia: header "Mensagens", busca inline, pill Adicionar amigos, faixa "na voz" horizontal, DM rows com timestamp relativo + "Você:", BottomUserBar, FAB compose, glyphs Canvas. Transicoes de rota realinhadas ao web (fade+slide-x EaseSpring, sem scale). Bug de build: PaddingValues nao aceita horizontal+bottom juntos -> start/end/bottom. Falta validar no device.
- 2026-06-20: M9 Contas+social ESCRITO + assembleDebug OK (falta validar no device). M9a registro (POST api/auth/register, AuthFields compartilhado, LoginScreen enxugado), M9b amigos (lista/pedidos/aceitar/remover), M9c perfil/conta (me/profile/updateProfile/changePassword + telas Settings/Account/ProfileEdit/UserProfile), M9d Home virou hub (rail+painel, faixa "na voz" entra na call, dot nao-lido+markSeen, FAB nova msg). Fix: KDoc `[userId...]` quebrava kspKotlin no ServerRepository.kt. Home shipou enxuto; extras (busca/timestamp/"Você:"/BottomUserBar/glyphs) viraram backlog.
- 2026-06-21: Sidebar v2 + uploads + editar/criar Constelacao (pedido fatiado, user pediu opcoes ao longo). FATIA 1 (visual): BottomUserBar virou barra full-width estilo Discord (banner do user faiscando atras + scrim; sem dot no avatar; sem botao sair; 1 engrenagem -> Settings; avatar->perfil, nome+chevron->status; carrega bannerUrl/bannerColor no HomeUiState/VM); sidebar mais transparente (alpha 0.5) + borda direita (drawBehind, paridade web border-r) + indicador ambar no tile ativo; copy "Sair da Astra"->"Sair do Astra". FATIA 2 (uploads usuario): coil-gif + AstraApplication=ImageLoaderFactory (GIF/WebP animam); infra generica UploadApi(multipart POST /api/upload)+UploadDtos+ImageUploader(IO, devolve URL); ProfileEdit trocou campo de URL por Android Photo Picker (avatar tocavel + chips Trocar foto/banner, preview do banner, remover banner, limites 5MB/8MB client-side, spinners); bannerUrl no updateProfile. FATIA 2c-1: criar Constelacao era so um '+' no canto (user achava que nao dava pra criar) -> virou 2 linhas claras "Forjar constelacao" / "Orbitar com convite". FATIA 2c-2: ServerEditScreen (icone via Photo Picker 5MB + renomear, so dono via ownerId vs TokenStore.currentUserId; engrenagem no header dos canais; PATCH /api/servers/:id); util compartilhado ImagePick.kt. EXTRA: Home observa ON_RESUME -> refreshProfile() (cache, sem rede) reflete avatar/banner editados no bottom bar. Transicoes: auditadas, sao globais no NavHost (toda tela herda) -> nada a fazer. Tudo assembleDebug OK, 5 commits. PENDENTE device: validar visual (banner atras, full-width), upload real, animacao GIF, editar server. Backlog: crop/zoom de imagem (escolhido "simples" por ora); icone ao forjar (hoje forja->edita); refresh do icone de servidor no rail da Home (so recarrega no proximo load).
- 2026-06-20: Tipografia/voz cosmica migrada do web pro nativo (user: "o Astra usa metaforas com espaco"). Criado `ui/AstraCopy.kt` = espelho de apps/web/src/lib/copy.ts (NOUN/ACTION/DESC/EMPTY/STATUS_LABEL/TOAST), ponto unico de copy. Sweep AGRESSIVO (escolha do user "agressivo + onboarding depois"): Servidores->Constelacoes (Forjar/Orbitar, membros->estrelas), Canais->Orbitas, Amigos->Estrelas (aba+title+empty), Mensagens->Sussurros (Home+DmList, "Iniciar sussurro"), status cosmico (Brilhando/Distante/Eclipse/Oculta/Apagada) em Home+Friends, "id interno"->"Coordenada Astra", "membro desde"->"estrela desde", "servidores em comum"->"constelacoes em comum". Erros de rede ("Sem conexao com o servidor") e fallbacks de nav-arg (invisiveis) ficaram literais de proposito. BACKLOG (prometido ao user): onboarding cosmico (espelhar CosmicOnboarding.tsx do web — slides constelacao/orbita/estrela/sussurro/cometa) pra ensinar os termos na 1a vez; sem ele o vocabulario agressivo pode confundir user novo. assembleDebug a validar.
- 2026-06-27: Roadmap estendido com a stack futura pedida pelo user (T4 KMP+Compose MP+Ktor+LiveKit+Conveyor+HW accel desktop; T5 plugins/IA resumir canal/LAN/eco-mode/swipe/E2EE; T6 Room/offline-first/Coroutines-Flow/Hilt; notas de perf Compose; tensoes de mentor E2EE-x-IA, Hilt/Retrofit-x-KMP, LiveKit-desktop, plugins-x-seguranca). So doc — nada implementado ainda.
- 2026-06-29: Leva grande de UI nativa + limpeza. Toolchain subiu p/ Kotlin 2.3.21 + AGP 9.2 + Gradle 9.4 + Room 2.8 (desbloqueou RikkaUI). RikkaUI adotada: Input Glow no composer, Skeleton Shimmer, AlertDialog/Dialog FadeScale via AstraDialog wrapper, Toast Scale no root. Gestao de categorias REMOVIDA (canais flat; criar canal preservado no header do servidor). Icone do Astra no launcher. Botao "+" do rail virou dropdown (criar servidor/grupo/convite). Swipe-to-reply (sempre L->R, haptic, mola) + swipe-back de borda (Modifier.edgeSwipeBack, reusa o pop do NavHost). Splash com glow pulsante que carrega o app por baixo (~1.8s + fade), sem subtitulo. Chat redesenhado estilo Discord: TUDO a esquerda, avatar+nome a esquerda, bolha uniforme, texto centralizado, agrupando msgs seguidas do mesmo autor. COMENTARIOS removidos do MONOREPO inteiro (mobile-native assembleDebug OK; apps/api + packages/types + apps/web tsc --noEmit verde; ~4.8k linhas de comentario fora, stripper respeitou strings/regex/diretivas). Roadmap ganhou T7 (otimizacao final). Falta validar tudo no device. Ponytail avaliada (redundante c/ karpathy) -> NAO instalada.
- 2026-06-18: M6d.2b 60fps config aplicada + transmite OK (nativo->web). Testado no EMULADOR: engasga (captura+encode de tela na CPU virtual nao sustenta fps alto = limitacao do emulador, NAO do codigo). Decidido nao tunar as cegas; deixei levantados os ajustes p/ device real (capture maxFps + codec H264 HW). => M6 = CORE NATIVO COMPLETO (voz, camera, screenshare). So a fluidez 60fps do screenshare fica pendente de 1 teste em celular fisico. Proximo grande marco: app desktop nativo (Compose Multiplatform reaproveita o Kotlin).
- 2026-06-29: Roadmap ganhou M10 (auditoria web -> paridade total no nativo: inventario primeiro, fechar gaps em fatias). Chat: densidade aumentada (avatar 34->42, texto ~17sp, bolha 86%, nome labelLarge) — escolha "Confortavel" do user.
- 2026-06-29: M10 inventario ESCRITO (docs/ASTRA-WEB-PARITY.md): varredura do apps/web (201 arquivos) x mobile-native, tabela por dominio com status+esforco. User escolheu 1a fatia = "Settings reais".
- 2026-06-29: M10 maratona LIGADA (user: "faca toda a camada M10"; calibragem: autonomo, pergunta so em forks reais de design). Backend confirmado com rota pra quase tudo (upload/gif/emojis/polls/push/fcm/roles/permissions/bans/discover/threads/bookmarks/translate/reminders/sessions/badges/notifications/search/wishes) -> porte de cliente.
- 2026-06-29: M10 fatia 2 — ANEXOS/IMAGENS completo (canal + DM, exibir + enviar + visor) + assembleDebug OK (falta device). AttachmentDto completo + core.model.Attachment; UploadApi.uploadMany; Room v3 (attachmentsJson, destructive). Fork de design (user): imagem = tile SOLTO (sem caixa), legenda em bolha separada; visor = zoom-open + pinch + double-tap + tap/back. MessageAttachments (grid 1=full/2+=colunas + chip de arquivo), Lightbox hospedado no ChatMessageList, composer com "+" Photo Picker (ImageOnly, ate 10) + PendingAttachmentsBar. Bolha de texto so quando ha texto/quote/fixado/reacao.
- 2026-06-29: M10 fatia 3 — GIF picker (canal + DM) ESCRITO + assembleDebug OK (falta device). GifApi (api/gif enabled/featured/search Giphy) + GifViewModel (featured + busca debounce) + GifPicker (overlay bottom-sheet, grid 3-col Coil). Composer ganhou botao "GIF". GIF entra como anexo image/gif (reusa render/visor; Coil-gif anima). DECISAO de escopo do "emoji": picker unicode completo PULADO (teclado do Android ja cobre); emojis CUSTOM do servidor (:nome: -> imagem) ADIADOS p/ casar com config-de-servidor/roles (precisam de serverId no chat + render inline InlineTextContent). 
- 2026-06-29: M10 fatia 4 — Discover (servidores publicos) ESCRITO + assembleDebug OK (falta device). DiscoverApi (GET api/discover?q + POST :id/join) + DiscoverViewModel (lista/busca debounce/join, 409=ja membro) + DiscoverScreen (cards icone/nome/descricao/estrelas + Entrar -> navega pros canais). Entrada no menu "+" do rail ("Descobrir", Lucide.Compass). Rota DISCOVER.
- 2026-06-29: M10 fatia 5 — Traduzir mensagem (canal + DM) ESCRITO + assembleDebug OK (falta device). TranslateApi (POST api/translate) + Translator @Singleton. Menu "Traduzir" -> PT (Claude Haiku backend, cache 24h, 50/dia), traducao sob a bolha (toggle). translations/translatingIds nos 2 VMs; ChatRow.translation. FALTA do M10 (proximos turnos): enquetes, mencoes, code blocks, perfil rico, settings restantes (sessoes/dados/idioma/wishing), edit-history, pull-to-refresh, deep link, busca, roles+emoji custom, threads, DM call. PUSH precisa do user (projeto Firebase + google-services.json).
- 2026-06-29: M10 fatia 1 — Settings reais ESCRITO + assembleDebug OK (falta device). PreferencesStore (DataStore astra_prefs: reduce-motion, vibracao, fonte sm/md/lg/xl, densidade compact/comfortable/spacious) + LocalAppPrefs provido na raiz. reduce-motion (escolha "pesadas"): StarField estatico, Reveal sem fade, splash sem pulso, transicoes viram fade curto, bolhas sem entrada/sweep; vibracao gateia haptic do swipe; fonte+densidade no chat. Telas AppearanceScreen (previa+fonte+densidade) e AccessibilityScreen (reduzir animacoes+vibracao) + PreferencesViewModel + rotas. Notificacoes adiado p/ fatia de Push (tela seria oca sem FCM). NAO portado de proposito: troca de accent/tema (tema fixo cosmico por design).
- 2026-07-01: M10 — leva 3b. PERFIL ESTILO DISCORD entregue: novo ProfileHero.kt compartilhado (banner imagem c/ zoom/posicao ou cor solida + scrim; avatar sobreposto na borda inferior com anel void + ponto de status; nome + subtitulo abaixo) usado no proprio perfil (ProfileSheet do HomeScreen, que so tinha avatar num Row) E no de outra pessoa (UserProfileScreen). Unifica o visual. assembleDebug verde. HAZE (blur) PESQUISADO mas NAO adicionado ainda: estavel = 1.7.2 (fev/2026; 2.0 e alpha, arquitetura nova c/ modulo haze-blur). API 1.7.x = Modifier.hazeSource(state) no fundo + Modifier.hazeEffect(state) no overlay. Decisao: nao meter as cegas — blur precisa de device p/ calibrar e o efeito que vale e vidro fosco no FUNDO do sheet de perfil (Home borrada atras), nao no banner. Fazer como fatia dedicada c/ o user vendo. FALTA: Haze frosted sheet; Baseline Profiles; Home iterativa.
- 2026-07-01: M10 — leva 3 (design + otimizacao, pedido do user). RENAME 'Estrela dos desejos' -> 'Estrela Cadente' (title+row). PERFIL DE OUTRA PESSOA (USER_PROFILE) entra como card cobrindo a tela vindo da DIREITA (slideInHorizontally full-width + fade; pop desliza de volta; respeita reduceMotion; API core). SKEW DE VERSAO MORTO NA RAIZ (backlog #1): descoberta via dependencyInsight = RikkaUI arrasta org.jetbrains.compose 1.10.0 -> forca androidx compose CORE (ui/foundation/animation/runtime) pra 1.10.0 em RUNTIME, mas compile ficava em 1.7.5 (BOM 2024.10.01); material3 e 1.3.1 nos dois lados (ok). Fix = constraints{} fixando o core em 1.10.0 no build.gradle (compile==runtime; APK empacotado nao muda pq runtime ja era 1.10.0). Mata a classe FlowRow E destrava APIs 1.8+ (SharedTransitionLayout, Haze). SPLASH ganhou constelacao se desenhando atras do nome (ConstellationGraphic reusado, gated em textAlpha>0 p/ nao quebrar o anti-jank). Pesquisa de libs (npx skills find + web): Haze (blur/vidro), SharedTransitionLayout (1.8+, agora compila), Lottie, AGSL shaders, Baseline Profiles. Decisoes via AskUserQuestion: alinhar BOM=sim, splash=constelacao, perfil=layout Discord completo. FALTA (proximo): layout Discord completo do perfil (ProfileSheet do proprio + UserProfileScreen) com blur Haze; Baseline Profiles. PRECISA teste no device (align e mudanca de build).
- 2026-07-01: CRASH DO PERFIL RESOLVIDO (o CrashReporter pagou na hora). Trace real: java.lang.NoSuchMethodError em FlowRow (assinatura ...FlowRowOverflow... nao existe no FlowLayoutKt em runtime) no device do user (Samsung A55, Android 16/API36). Diagnostico: compilado contra androidx.compose.foundation-layout 1.7 (compose BOM 2024.10.01) mas a foundation-layout empacotada em runtime e mais antiga = skew de versao arrastado por lib de terceiro (provavel dev.rikkaui:0.3.0; nao ha foundation-layout explicito no catalogo, vem transitivo via material3+rikkaui). NAO era logica (por isso o chute do Slider falhou) — era binario. Latente em TODO FlowRow (Perfil/Aparencia/reactions do chat); Perfil so batia primeiro. FIX: FlowRowCompat.kt (wrap manual via Layout, API core estavel imune ao skew) substitui os 3 FlowRow, preservando comportamento. assembleDebug verde. RAIZ p/ backlog: investigar/forcar/excluir a foundation-layout transitiva da RikkaUI (resolutionStrategy.force ou exclude) — enquanto isso, nao usar androidx FlowRow/FlowColumn, usar FlowRowCompat.
- 2026-07-01: M10 — leva 2 (pedido do user: 6 frentes). (1) CRASH do Perfil PERSISTE (chute do Slider errado) -> como tudo e defensivo e nao ha device no adb, criei CrashReporter (UncaughtExceptionHandler no Application grava stacktrace em filesDir/last_crash.txt; MainActivity no launch seguinte mostra CrashScreen com trace mono + 'Copiar erro'). Vira erro copiavel + rede de seguranca permanente; user reproduz e manda o trace p/ fix real. (2) Cor do nome MOVIDA pra dentro de Personalizacao: NameColorsScreen -> NameColorsSection (sem chrome/scroll proprio), entre Perfil e Aparencia; removido row+rota NAME_COLORS. (3) Acento: titulo/label 'Personalização' (com ç/ã) + secoes '— cor do nome'/'— aparência'. (4) Splash anti-jank: delay(1000) de tela vazia (so o fundo, sem texto) ANTES de animar, p/ o device terminar o cold-start; E trocado o CosmicBackground do splash por Box void solido (o StarField era Canvas full-screen 60fps competindo por frames no cold-start) — o starfield segue nas telas reais. (5) Skills: npx skills find -> chrisbanes/compose-recomposition-performance (Google) referenciada. (6) Revisao: StarField e listas ja eficientes (draw-phase only, keys estaveis, mapeamentos memoizados); autor ja cuidou de perf. Achados p/ backlog: repos com catch(Exception) engolem CancellationException; set 'animated' do ChatMessageList cresce sem limite; lambdas instaveis no MessageBubble; possivel OOM de banner data-uri grande (candidato ao crash); Baseline Profiles = maior ganho de cold-start (a fazer). 6 commits, assembleDebug verde cada.
- 2026-06-30: M10 — aba unica Personalizacao + splash suave + fix crash do Perfil (pedido do user, 4 frentes; 3 commits, assembleDebug verde cada). (1) CRASH ao abrir Perfil: coercao de bannerScale/bannerPositionY na origem (applyProfile) + belt no value do Slider — chute = Slider de zoom estourando com valor fora de 50-200 (ex.: contrato web guarda multiplicador, nao percentual). Se user nao tiver banner e ainda crashar, e outra coisa -> precisa logcat. (2) MERGE: Perfil + Aparencia -> uma so aba 'Personalizacao'. ProfileEditScreen/AppearanceScreen viram secoes reutilizaveis (ProfileEditSection/AppearanceSection) num scroll unico; 'Conta' separada; rotas PROFILE_EDIT+APPEARANCE unificadas em PERSONALIZATION (Home e Settings apontam pra ela). (3) SPLASH: troca AnimatedVisibility (entrada instantanea) por Animatable proprio — nome 'Astra' fade+zoom 0.92->1, segura ~0.8s, saida = fade do overlay + lift 1.06 revelando o app; tudo graphicsLayer (GPU, sem travar), respeita reduceMotion. Decisoes via AskUserQuestion. FALTA (4a frente, decidida = estrutura Discord + skin Astra, iterativo com orientacao do user): redesenhar Home p/ tamanhos iguais estilo Discord incluindo botao de perfil.
- 2026-06-29: M10 fatia 13 — Mencoes destacadas no chat ESCRITO + assembleDebug OK (falta device). Pequeno contido. Conteudo guarda @username literal (backend regex @[a-z0-9_]+); destaco os tokens em accent + FontWeight.Medium via buildAnnotatedString (mentionAnnotated em Chat.kt, key content+accent). Canal + DM. Sem resolver IDs (destaca todo @token) — aproximacao limpa, sem backend. NAO feito: realce especial da msg que menciona VOCE (precisa do meu username + array mentions). Pequenos restantes: pull-to-refresh (adiado), busca global (medio: tela multi-secao + entrada no Home), deep link convite.
- 2026-06-29: M10 fatia 12 — Historico de edicoes (canal) ESCRITO + assembleDebug OK (falta device). Primeiro dos "pequenos". Mensagem editada -> acao "Historico de edicoes" no menu (so se edited) -> dialog com versoes anteriores (GET .../messages/:mid/edits, ate 20, recente primeiro) + tempo relativo. MessageEditDto/MessageEdit; ChannelApi.edits; repo.editHistory; VM load/closeEditHistory; onHistory em MessageBubble/menu/ChatMessageList. Canal-only. Pequenos restantes: pull-to-refresh (decidi adiar p/ lugar adequado - chat e reverseLayout, Home e complexo), mencoes highlight (precisa resolver usernames dos ids), busca, deep link de convite.
- 2026-06-29: M10 fatia 11 — Gerenciar cor do nome (por servidor) ESCRITO + assembleDebug OK (falta device). Fecha o ciclo da fatia 7 (que so exibia). Aba "Cor do nome": lista servidores (GET /api/servers), card expande com 10 presets + hex custom (#RRGGBB) c/ preview, Aplicar/Limpar. PATCH /api/servers/:id/my-color {nameColor|null}. Espelha web (so hex solido; gradiente fica fora). ServerDto +isGroup; MyColorDtos; ServerApi.setMyColor; NameColorsViewModel (Api direto) + Screen. NAO pre-carrega cor atual (web tbm nao; ServerDto nao traz nameColor). Abas web ainda fora: Notifications(prefs, depende Push), Language(i18n, skip). Settings essencialmente em paridade.
- 2026-06-29: M10 fatia 10 — Personalizacao de perfil (fonte, posicao/zoom banner, tema) ESCRITO + assembleDebug OK (falta device). User escolheu 3 das 4 partes (sem presets de gradiente no banner); embutido na tela de Perfil. Fonte de exibicao: picker 4 fontes nativas (serif/sans/mono/manuscrita -> FontFamily; fallback p/ as 8 ids do web) no nome do edit + UserProfile. Posicao+zoom banner: drag vertical (bannerPositionY) + slider 50-200% (bannerScale) via BiasAlignment+graphicsLayer. Tema do perfil: 12 gradientes (linear-gradient -> Brush via parseGradientBrush) pintam fundo do UserProfile c/ scrim + "nenhum". Data: ProfileUserDto/UpdateProfileRequest/Profile +profileTheme/bannerPositionY/bannerScale/displayFont; updateProfile estendido; ProfileLook.kt helper. PATCH /api/profile (formato linear-gradient valida no BANNER_COLOR_RE). Abas web ainda fora: Notifications(prefs, depende Push/Firebase), Language(i18n grande), NameColors(gerenciar propria cor).
- 2026-06-29: M10 fatia 9 — Abas de settings (Sessoes, Estrela dos desejos, Dados) ESCRITO + assembleDebug OK (falta device). User: "tente fazer as abas que nao temos ainda do app web". Sessoes (GET/DELETE /api/sessions + revoke-others; parse UA -> rotulo+movel; badge "este"; encerrar individual + outras c/ confirm). Estrela dos desejos (GET/POST /api/wishes feed global paginado; compor 4-500 + contador; lista avatar/nome/@/tempo; ver mais antigos). Dados (stub exportar/apagar "em breve", igual web). SessionApi+VM, WishApi+VM (padrao leve tipo Discover: Api direto no VM). SettingsScreen +grupos "conta e seguranca"/"comunidade" +rotas. PULADO de proposito: Idioma (i18n grande), Personalizacao (banner/gradientes/fonte display -> fork de design, merece conversa), Notificacoes prefs (push depende do Firebase do user). Abas web ainda fora: Customization, Notifications(prefs), Language, NameColors(gerenciar a propria cor).
- 2026-06-29: M10 fatia 8 — Aparencia completa (accent/fundo/presets dinamicos) ESCRITO + assembleDebug OK (falta device). User pediu "traga tudo da aparencia". REVERTE a decisao anterior de tema fixo: AstraTheme virou dinamico (recebe accentId/bgId -> deriva accent*/void/base/raised; resto fixo; Material+Rikka schemes derivam do accent). ThemeOptions.kt espelha lib/theme.ts (18 accents, 10 fundos, 7 presets). AppPrefs/PreferencesStore +accentId/bgId (DataStore) +setTheme; default white+void = visual atual (nada regride). AppearanceScreen +3 blocos (tema rapido/cor destaque/fundo). Persistencia local, sem sync backend (PATCH /profile/preferences) por ora. Tema muda app inteiro via LocalAstraColors (static local -> recompoe subtree). Falta do "perfil rico": badges, statusEmoji/recado. Settings restantes (sessoes/dados/wishing) seguem.
- 2026-06-29: M10 fatia 7 — Cor de nome no chat (nameColor por servidor) ESCRITO + assembleDebug OK (falta device). User escolheu essa entre as opcoes de "perfil rico". Autor das mensagens no canal usa a cor do membro; suporta hex solido E gradiente (gradient:ANGLE:#a:#b -> Brush no texto via TextStyle.brush). authorColor ja vinha no payload (REST historico/envio + socket) -> so faltava cliente: DTO/model/MessageEntity (Room v5) + mapping repo + parseNameColor (sealed Solid/Gradient) + render no MessageBubble (fallback accent). DM fica accent (sem nameColor fora de servidor). Resto do bundle "perfil rico" (badges, statusEmoji/recado) e settings (sessoes/dados/wishing) seguem pendentes.
- 2026-06-29: M10 fatia 6 — Enquetes (polls) ESCRITO + assembleDebug OK (falta device). Canal-only (DM backend mais magro). PollCard na mensagem: pergunta, opcoes c/ barra de preenchimento animada (accent, gated reduce-motion), contagem + %, voto por toque, badge "encerrada", rodape total/expiracao/encerrar. PollComposer (bottom-sheet, botao 📊 no input): pergunta, 2-8 opcoes add/remove, multipla escolha, duracao (sem limite/1h/6h/24h/3d/7d). Data: PollDto/CreatePollRequest/VoteRequest/PollUpdateDto; ChannelApi create/vote/close; SocketManager poll_updated; MessageEntity.pollJson (Room v4 destrutivo); repo map em toDomain/toEntity/toChannelMessage + coletor socket; VM votePoll/closePoll/createPoll. FALTA do M10: perfil rico, settings restantes (sessoes/dados/idioma/wishing), edit-history, pull-to-refresh, mencoes, deep link, busca, roles+emoji custom, threads, DM call, PUSH (precisa Firebase do user).
- 2026-07-01: M10 — leva 4 (design + perf, pedido do user via 2 prints Discord). (1) PERFIL: botao "Editar perfil" SUBIU pro topo do ProfileSheet (logo abaixo do nome, AstraButton Primary/ambar) espelhando o Discord; removido do rodape (era duplicata); cards editoriais mantidos; SEM Orbs/Lista de desejos (nao existem no Umbra). Fork resolvido via AskUserQuestion = "Layout Discord, pele Umbra". (2) HAZE FROSTED SHEET: dev.chrisbanes.haze 1.7.2 no catalogo; Home = hazeSource; ProfileSheet com containerColor transparente + dragHandle=null (handle manual) + hazeEffect (blurRadius 24, tint overlay .5, noiseFactor 0) -> Home borra atras do card (vidro fosco iOS/Discord). Gotcha compile: astraColors e getter @Composable -> hoistar a cor (overlayColor) fora do bloco haze (lambda normal). compileSdk 35->36 (exigido transitivamente por androidx.activity 1.12/navigationevent que o Haze arrasta; targetSdk fica 35, runtime inalterado; preferido a forcar downgrade p/ nao repetir skew). assembleDebug verde. FALTA device p/ validar blur real (cross-window do ModalBottomSheet) e calibrar blurRadius/alpha. (3) BASELINE PROFILE: user escolheu "so documentar". FATO: profileinstaller 1.4.0 JA no classpath -> AGP mescla+instala os baseline profiles das libs (Compose/AndroidX) no 1o run = startup do Compose JA e AOT. So falta o app-especifico (Main->Splash->Home->load), que SO gera rodando Macrobenchmark num device. RECEITA p/ depois: (a) add plugin `androidx.baselineprofile` no :app (versao casada c/ AGP 9.2, conferir maven.google) + criar modulo `:baselineprofile` (com.android.test) com BaselineProfileGenerator do startup journey; (b) herdar os opt-outs do toolchain (builtInKotlin/newDsl=false) no modulo novo e validar compile; (c) A55 via USB (depuracao ligada) -> `gradlew :app:generateBaselineProfile` gera app/src/main/generated/baselineProfiles/baseline-prof.txt; (d) commitar (AGP empacota em assets/dexopt/baseline.prof, profileinstaller instala). Proximo: Home iterativa + self-host LiveKit.
- 2026-07-02: M10 — SETTINGS PARITY FECHADA (pedido "trazer tudo das configuracoes do web"). Inventario real: web tem 11 abas; nativo ja tinha 8 em paridade (Customization = fatia 10 antiga; Data = stub no web tambem). Gaps reais eram 3 + idioma. (A) NOTIFICACOES prefs (6ce2f53): NotificationsApi+DTOs (prefs/feed/unread/fcm), tela com 4 toggles de tipo + sons + horario silencioso (pickers 00-23) + push de teste; VM otimista; GOTCHA explicitNulls=false: limpar quiet hours exige JsonObject com JsonNull explicito (DTO normal omite nulls = merge parcial correto). (B) SINO/FEED (e0ae425): onBell={} stub virou NotificationsFeedScreen (paginacao cursor, frase por tipo, marcar lidas, tocar navega canal/dm; payload JsonObject achatado leniente) + badge unreadNotifs no BottomUserBar (GET unread no init+ON_RESUME). (C) PUSH FCM (5674719): codigo 100% pronto compilando SEM google-services.json (plugin google-services 4.5.0 CONDICIONAL: so aplica se o json existir; firebase-bom 34.15.0); AstraMessagingService (onNewToken->POST fcm-token; onMessageReceived so foreground, background o sistema exibe a notification-block); PushChannels mentions/dms/general (paridade pushNative.ts + lib/fcm.ts); PushRegistrar no-op sem FirebaseApp; permissao POST_NOTIFICATIONS pedida na Home (33+); ic_stat_astra (backend referencia); guia docs/PUSH-FCM.md (5 min: registrar app.astra.mobile.debug E app.astra.mobile no projeto Firebase EXISTENTE — user ja tem FIREBASE_SERVICE_ACCOUNT na Railway = lado servidor vivo). IDIOMA: pulado por escolha (PT-only, i18n nao vale pro publico atual). JULGAMENTO de techs pesquisadas pelo user (registro): Lottie=pontual ok / Rive=nao (sem designer) / AGSL+Canvas+RenderEffect=ja em uso nas camadas certas / Lazy*=ok ja otimizado / StaggeredGrid=guardar (galeria futura) / Paging3=NAO (cursor manual + reverseLayout e o padrao certo pra chat) / NestedScrollConnection=guardar (collapsing header do perfil). FALTA settings: nada. FALTA push: user colar o json (guia) e validar no device.
- 2026-07-03: LEVA 8 — composer limpo + email real + senha Google + perf estrutural (4 frentes do user, decisoes via AskUserQuestion; 4 commits + doc, builds verdes). (A) COMPOSER MINIMALISTA (c71a99d): [+] [input] [enviar]; o "+" abre DropdownMenu ancorado (flipa pra cima) com Fotos/GIF/Enquete/Emoji (icones Lucide Image/Film/ChartColumn/Smile) — user escolheu emoji DENTRO do popup; canal e DM. (B) PERF (45fee65): gargalo achado = cada tela tinha CosmicBackground proprio (2 shaders + 2 canvas full-screen animando nas transicoes). Fix estrutural: CosmicBackdrop GLOBAL unico atras do NavHost (AstraApp); CosmicBackground das 24 telas virou shim Box transparente (zero churn); ProfileSheet usa Backdrop direto (overlay opaco); fbm 4->3 oitavas; toque reativo exige down NAO consumido (senao todo scroll acenderia o glow global). Ceu fica parado nas transicoes (escolha do user, padrao Discord/iOS). Skills pesquisadas (npx skills find): nada de perf Compose alem do que ja aplicamos. (C) SENHA OBRIGATORIA GOOGLE (5120631): POST /auth/password/set (SetPasswordSchema novo no @astra/types; so se passwordHash null) + CreatePasswordGate overlay full-screen na Home (back bloqueado, 2 campos, validacao espelhando o schema); hasPassword ja fluia no me(). (D) EMAIL DEVE EXISTIR (15905ec): verificacao por codigo 6 digitos. Migration 0019 (emailVerifiedAt/emailCode/emailCodeExpiresAt; contas existentes carimbadas; Railway roda db:migrate no deploy — confirmado no railway.json, entao migration escrita A MAO e segura). nodemailer/Gmail (GMAIL_USER/GMAIL_APP_PASSWORD opcionais; sem eles registro nasce verificado, nada trava; resend auto-verifica se mailer off). Login Google carimba (posse provada). App: gate na Home (padrao onboarding) -> VerifyEmailScreen (codigo, reenviar, "sair da conta" como escape de email fake, back bloqueado). GUIA pro user: docs/EMAIL-VERIFY.md (senha de app do Gmail + 2 vars na Railway). FALTA user: executar o guia do Gmail. FALTA device (leva 8): menu do composer, fluidez transicao/teclado pos-backdrop, gate de senha com conta Google, registro novo com codigo.
- 2026-07-02: LEVA 7 — polish geral + aurora v2 + 3 techs novas (pedido "polir tudo + sugerir techs + melhorar aurora"; 9 frentes escolhidas via AskUserQuestion, 7 commits, assembleDebug verde cada). AURORA v2 (1a74431): shader FBM (value-noise 4 oitavas, tempo anda em CIRCULO no espaco de ruido cos/sin*raio -> loop 60s sem salto; corpo assimetrico + raios de alta freq), parallax por acelerometro (dois low-pass rapido-lento = re-centra sozinho; so em RESUMED; camadas aurora 0.02uv/twinkles 10dp/bg 5dp) e toque reativo SO onde fundo e protagonista (login/registro/onboarding/perfil): glow segue dedo + anel na soltura (Animatables proprios, sem depender do time que loopa). HAPTICS (664db8b): AstraSwitch central ToggleOn/Off (ToggleRows migrados), Confirm no enviar, controles de call, atender/recusar, rail SegmentTick — tudo atras de prefs.haptics; sem vibra dupla no proprio toggle Vibracao. PREDICTIVE BACK + ICONE TEMATICO (d1d1a4f): enableOnBackInvokedCallback + <monochrome> (estrela do ic_stat_astra escalada 52/108dp). EMPTY STATES (31eb068): EmptyState (constelacao) substituiu texto seco em chat canal/DM, feed notifs, discover, wishing, Home (DMs sem busca + canais). EMOJI PICKER (505f459): androidx.emoji2-emojipicker 1.5.0 no painel editorial (padrao GifPicker); "+" nas QuickReactions do long-press (canal) + botao no composer (canal+DM). NOTIF CONVERSACIONAL (dffa5a0): backend FCM virou DATA-ONLY (title/body/icon/tag/sender/notifChannel no data; PushPayload +sender) — antes o notification-block exibia sozinho em background e o app nem rodava; DmNotifier (MessagingStyle + Person avatar circular + historico continuado via extractMessagingStyleFromNotification + RemoteInput MUTABLE) + ReplyReceiver (goAsync, POST dm/messages, reposta a notificacao senao o spinner nunca para); so afeta nativo (fcmTokens), web push intocado. DIRECT SHARE (cae522e): DmShortcuts (pushDynamicShortcut longLived+Person+categoria, avatar ou inicial ambar) publicado ao abrir DM; shortcuts.xml share-target text/image + intent-filter SEND; MainActivity le EXTRA_SHORTCUT_ID -> DeepLinkBus.pendingShare -> AstraApp navega -> DmChatScreen consome (texto=rascunho, imagem=anexo). Backlog: share generico sem alvo so abre o app (picker de conversa depois). FALTA device (leva 7): aurora nova (intensidade/parallax/toque), haptics, icone tematico, emoji picker (tema escuro da view), push DM fechado + responder da bandeja, direct share texto+foto. — a Home ja esta em paridade com o Discord (print confirma); user priorizou LiveKit. Registrado que presenca/contadores por DM exigiriam backend novo (modelo Conversation nao tem status), nao sao 'so visual'. SELF-HOST LIVEKIT: user confirmou Oracle Cloud (Sao Paulo), com exigencia de '60fps garantido + qualidade'. Achado-chave: backend (voice.ts le env.LIVEKIT_URL/KEY/SECRET; clients pegam url+token do /token) = ZERO mudanca de codigo; self-host = subir SFU numa VM (IP publico + UDP + TLS; Railway e HTTP-only, nao serve) + trocar 3 env vars na Railway. APP tunado pro 60fps (commit): screenshare publish agora H264 (encoder HW sustenta 60fps, vs VP8 software que engasgava — era o ajuste anotado no M6d.2b) + degradationPreference MAINTAIN_FRAMERATE (derruba resolucao antes de fps) alem de maxFps 60/8Mbps/simulcast off ja existentes. Nota de mentor ao user: SFU so encaminha (nunca recodifica) -> fps/qualidade saem dos aparelhos; '60fps garantido' = priorizar framerate no publish + servidor sem gargalo (UDP aberto + regiao perto), NAO da pra prometer contra rede ruim (congestion control derruba de proposito). SERVIDOR: guia completa em docs/LIVEKIT-SELFHOST.md (runbook Oracle p/ non-devops): conta+regiao SP, VM A1 ARM Always Free, portas nos DOIS filtros (Security List da VCN + iptables da instancia — o gotcha classico do Oracle), DuckDNS gratis + Caddy TLS, gerador oficial `livekit/generate` (monta Caddy+LiveKit+Redis + imprime API key/secret), docker compose up, trocar LIVEKIT_URL/KEY/SECRET na Railway, testar, troubleshooting. NAO commitei configs (contem secret + sao por-host; geradas na VM). Portas: 80/443/7881 tcp + 3478/50000-60000 udp. FALTA: user executar o runbook na conta dele + validar call no device.
- 2026-07-03: LEVA 8.5 — deploy consertado + toque global + bug de sessao. (1) DEPLOY QUEBRADO (5aa3845): 15905ec adicionou nodemailer sem atualizar o package-lock.json da RAIZ; a fase de install da Railway (npm ci via Nixpacks) recusa lockfile dessincronizado -> build falhava e a Railway SEGUROU o deploy anterior (por isso "rota nao encontrada" nas rotas de email do app). Diagnostico por curl triangulando qual commit estava no ar (/password/set 401=existe vs /email/resend 404=nao existe); apos o fix, resend virou 401 = rotas no ar. LICAO PERMANENTE: mudou package.json de qualquer workspace -> commitar o lock da raiz JUNTO. Gotcha de debug: PS 5.1 mangla -d com JSON no curl.exe -> 500 falso do error handler; testar existencia de rota SEM body. (2) RIPPLE COMPACTO (ef74601): glow falloff 9->28, anel 0.16 uv/s + decay 4.5 + width 60 ("praticamente do tamanho do clique"). (3) BUG DE SESSAO (caa9d3b): deslogar e registrar conta nova caia na conta ANTIGA — UserRepositoryImpl singleton com cached Profile que o logout() nao limpava (me() sem forceRefresh devolvia o perfil anterior, com emailVerifiedAt carimbado = pulava ate a tela de codigo). Fix: clearCache() chamado em toda troca de sessao (logout/login/registro/Google). (4) TOQUE NO CEU GLOBAL + TOGGLE (df214cb): o detector vivia no canvas da aurora ATRAS do conteudo -> telas com lista (chat/DM/servidores) bloqueavam o hit-test e o efeito so aparecia em area descoberta. Movido pro Box PAI do CosmicBackdrop (hit-path de toda a UI) com logica "tap no vazio": dispara so se NENHUM filho consumiu o gesto e o dedo nao passou do touch slop (click/scroll/campo de texto ficam imunes); TouchFx hoisted, AuroraShader so desenha; glow virou pulso (90ms sobe/450ms desce) ja que nao ha mais "segurar no vazio" garantido. Toggle "Toque no ceu" na aba de animacoes (anim_sky_touch, so visivel com aurora on). SELF-HOST: signup da Oracle FALHOU pro user; decisao de provedor alternativo em aberto (requisitos: regiao BR p/ ping, UDP aberto, custo minimo, pagamento acessivel p/ estudante). FALTA device: ripple global nas telas de chat, toggle novo, fluxo logout->registro limpo.
- 2026-07-04: LEVA 9 — auditoria "batata" + silenciar tudo (escolhas do user via AskUserQuestion; self-host PAUSADO: user sem cartao de credito, Oracle/AWS/Vultr exigem; plano B registrado = Hostinger VPS BR ~R$27/mes via PIX). (1) AUDITORIA LISTAS+IMAGENS (fa99d06): listas ja estavam certas (12 items() com key estavel, parse markdown/mencao memoizado, strong skipping K2.3) — NADA mudado la (honestidade > churn). Ganho real: Coil sem ajuste low-end -> em isLowRamDevice agora allowRgb565 (metade da RAM por imagem opaca) + memory cache 25%->15% do heap; aparelho normal intocado. (2) MUTE BACKEND (d71b52c): DM = mutedByA/B na DMConversation (padrao lastReadByA/B), PUT/DELETE /dm/:id/mute (preserva updatedAt pra nao reordenar lista), lista GET /dm devolve muted, envio pula notify() inteiro se receptor mutou (socket new_dm continua); SERVIDOR = tabela ServerNotifPref + GET/PUT/DELETE /servers/:id/notification-pref; getNotifModesFor resolve em camadas (pref explicita do canal MESMO 'all' > servidor > 'all'); migration 0020 a mao (journal idx 20). (3) MUTE APP (96160d7): sino no header do canal (menu Tudo/So mencoes/Silenciado/Padrao do servidor), sino toggle no header da DM, "Silenciar constelacao" no long-press da rail; badges limpos no cliente (Home e DmList excluem mutados do unread, socket incoming mutado nao acende, canal mutado = alpha 0.55 + BellOff); Home busca prefs no load() em paralelo e recalcula local no toggle. Otimista em tudo (erro reverte). (4) BUGFIX pre-leva: logout nao limpava cache singleton do perfil -> registrar conta nova caia na conta antiga (caa9d3b, clearCache() em toda troca de sessao) + toque no ceu agora funciona em QUALQUER fundo vazio (df214cb: deteccao movida pro Box pai do CosmicBackdrop com logica tap-no-vazio — canvas atras do conteudo nunca recebia toque em tela com lista; toggle novo "Toque no ceu" na aba de animacoes, anim_sky_touch). ACHADO pro perfil rico: backend de badges JA PRONTO (0018: globais derivadas Bot/Pioneiro + badges de servidor com grants/permissoes, rotas /users/:id/badges e /servers/:id/badges) e users.customStatus ja existe — feature e majoritariamente consumo no app. FALTA device (leva 9): sino canal/DM/servidor, badges limpos, fluxo mute end-to-end com push real, RGB565 em aparelho fraco.
- 2026-07-04: LEVA 10 — PERFIL RICO fechado (badges + recado + gestao; design via AskUserQuestion = chips editoriais / recado em perfil+listas / gestao completa no app). ACHADO que guiou a leva: backend JA ESTAVA PRONTO (0018 badges + users.customStatus + PATCH /friends/custom-status) — 95% da leva foi consumo no app. API (71b3010): customStatus adicionado ao me() e ao GET /users/:id (coluna e rota de setar ja existiam; faltava fluir na leitura). APP (d7b7d0a): BadgeChips novo em ui/components/Badges.kt (capsulas emoji+nome, borda na cor da badge, tap = mini-card com descricao/origem); UserProfileScreen mostra recado em aspas + chips entre hero e bio (badges buscadas em paralelo, best-effort); ProfileSheet ganhou recado EDITAVEL inline (EditorialField + emoji picker + limpar/salvar; otimista + me(force) no sucesso; limpar manda "" pq explicitNulls=false omitiria null e o Zod exige a chave) + chips; lista de amigos JA exibia recado (customStatus ?: presenca, zero mudanca); ServerEdit ganhou secao "insignias" (criar com emoji/nome/descricao/7 swatches de cor, apagar com confirmacao, conceder/revogar via dialog com switch por membro; tudo otimista com revert). DTOs BadgeDtos.kt + BadgesApi (users/:id/badges + CRUD servers/:id/badges) + provider no NetworkModule. FORA desta leva: recado na lista de MEMBROS do servidor (rota /members nao devolve customStatus; avaliar depois). FALTA device (leva 10): chips no perfil proprio/alheio, editar recado com emoji, criar/conceder insignia e ver no perfil do membro.
- 2026-07-04: LEVA 10.5 — techs do user julgadas + APK RELEASE destravado (b855ca7). JULGAMENTO (pesquisa do user): LeakCanary = JA ESTAVA no build (debugImplementation 2.14, notifica vazamento em teste); RGB_565 = ja aplicado (fa99d06, low-RAM only); R8 = ja configurado mas dormia (user instalava debug); graphicsLayer = ja usado certo (Reveal alpha/translationY em draw-phase, ProfileHero scale, aurora/starfield leem estado no Canvas); overdraw = estrutural resolvido na leva 8 (backdrop global unico), falta so medir no device (Dev options > Debug GPU overdraw). ACAO: assinatura de release LOCAL — keystore.properties + astra-release.keystore gerados na maquina (senha aleatoria, ambos gitignored com fallback pra debug-signing se ausentes) -> assembleRelease passou no R8 DE PRIMEIRA (regras kotlinx.serialization existentes bastaram), APK 70.2MB -> 49.6MB (-29%). User agora instala Astra-release.apk (R8 + Compose modo rapido = maior salto de fluidez disponivel). AVISOS: push no release exige registrar app.astra.mobile no Firebase (docs/PUSH-FCM.md; o .debug e pacote diferente) e LeakCanary continua so no debug. BACKLOG GPU/CPU (ordem de impacto): (1) Baseline Profile app-especifico (receita na leva 4, precisa device USB); (2) aurora em layer de resolucao reduzida (~1/2 res + upscale; shader e desfocado por natureza, corta ate 75% do custo/pixel — casa com modo desempenho automatico via isLowRamDevice que o user deixou na mesa); (3) medir antes de otimizar mais: overdraw debugger + Profile HWUI + LeakCanary.
- 2026-07-04: LEVA 10.6 — aurora em MEIA RESOLUCAO (9115615; plano GPU do user: meia-res -> medir -> baseline profile). Tecnica: canvas do AuroraShader medido na METADE (Modifier.layout com Constraints.fixed(w/2,h/2), reporta tamanho cheio pro pai) + graphicsLayer scale 2x transformOrigin(0,0) + CompositingStrategy.Offscreen — GOTCHA central: sem o Offscreen o RenderNode reexecuta o draw ja transformado e o shader roda por pixel FINAL (ganho zero); com ele rasteriza no buffer meio-tamanho (~2.6MB) e escala a textura bilinear = AGSL calcula ~25% dos pixels. Shader e resolucao-independente (uv=fragCoord/iResolution) -> toque/parallax alinhados sem ajuste; estrelas/meteoros/conteudo seguem res cheia; 1px de borda inferior em altura impar e invisivel (aurora ja e transparente no rodape). PROXIMO (ordem do user): (1) MEDIR no device — Dev options > Debug GPU overdraw (vermelho = investigar), Profile HWUI rendering (barras < linha verde = 60fps), LeakCanary ja vigia no debug; user reporta e a gente caca o que aparecer; (2) BASELINE PROFILE app-especifico — receita completa na leva 4 (plugin androidx.baselineprofile + modulo :baselineprofile com.android.test + A55 no USB -> generateBaselineProfile -> commitar baseline-prof.txt). Modo desempenho automatico (isLowRamDevice) segue na mesa como complemento.
- 2026-07-04: LEVA 10.7 — BASELINE PROFILE GERADO NO A55 e empacotado (1b7d2a9). Startup agora nasce AOT: 23.499 regras coletadas do journey real de abertura -> app/src/main/baseline-prof.txt -> AGP compila pra assets/dexopt/baseline.prof (14KB no APK, R8-remapeado) + profileinstaller explicito instala no 1o run. CAMINHO TORTO documentado: plugin androidx.baselineprofile 1.5.0-alpha06 (unico que fala AGP 9.x) QUEBRA o KSP na variante nonMinified que ele cria em modo legado ("Illegal char <?> ... provider(?)" ao resolver deps de kspNonMinifiedReleaseKotlin); benchmark alpha06 e KSP 2.3.9 sao os MAIS NOVOS dos dois lados -> sem bump possivel -> FLUXO MANUAL classico: build type "benchmark" comum no :app (initWith(release), minify off pra regras sairem legiveis; R8 remapeia no empacote) + overlay <profileable shell> + modulo :baselineprofile (com.android.test, self-instrumenting, BaselineProfileRule) -> :baselineprofile:connectedBenchmarkAndroidTest -> copia manual do txt. GOTCHAS: (a) plugin com versao em modulo secundario = "already on classpath with unknown version" -> apply false no raiz; (b) build type custom de modulo de teste nao herda assinatura -> INSTALL_PARSE_FAILED_NO_CERTIFICATES (signingConfig debug explicito); (c) Select-String com -First no PS mata o pipeline e derruba o gradle no meio (filtro casava com o nome do modulo) — log completo em arquivo, filtrar depois; (d) config Android "USB controlado por" e irrelevante pro adb (o que vale e a autorizacao de depuracao). REGENERAR o profile: so quando o fluxo de abertura mudar muito (novo gate/tela inicial) — mesmo comando com o device plugado. FALTA user: instalar Astra-release.apk e sentir abertura/fluidez (1o run instala o profile; 2a abertura em diante e o retrato real).
