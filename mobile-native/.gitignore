*.iml
.gradle/
/local.properties
/.idea/
/build/
/app/build/
/captures
.externalNativeBuild
.cxx
*.apk
*.keystore
!debug.keystore
.kotlin/

# Firebase (por-dev, contem ids do projeto)
app/google-services.json
keystore.properties

# ffmpeg empacotado (captura DXGI 60fps) — grande, baixado via :desktopApp:fetchFfmpeg
desktopApp/appResources/windows/ffmpeg.exe
