package app.astra.mobile.ui.theme

import androidx.compose.material3.Typography

// Tipografia base do Material3 por enquanto. Depois plugamos as fontes
// editoriais do Astra (serif display + mono pra metadata) via FontFamily.
val AstraTypography = Typography()
